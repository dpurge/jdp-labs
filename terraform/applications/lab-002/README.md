# LAB-001 Use remote terraform state

This lab depends on resources created in `lab-001`.

The goal of this lab:

- use the remote terraform state created in `lab-001`.

```sh
terraform init
terraform plan
terraform apply
terraform destroy
```
