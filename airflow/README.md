# Airflow Lab

Configure local environment:

```sh
echo -e "AIRFLOW_UID=$(id -u)\nAIRFLOW_GID=0" > .env
```

Login in the web interface at `http://localhost:8080/`:

- User: airflow
- Password: airflow
